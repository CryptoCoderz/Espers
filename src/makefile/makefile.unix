# Copyright (c) 2009-2010 Satoshi Nakamoto
# Distributed under the MIT/X11 software license, see the accompanying
# file COPYING or http://www.opensource.org/licenses/mit-license.php.

USE_UPNP:=0
USE_WALLET:=1

LINK:=$(CXX)
ARCH:=$(system lscpu | head -n 1 | awk '{print $2}')

DEFS=-DBOOST_SPIRIT_THREADSAFE

DEFS += $(addprefix -I,$(CURDIR) $(CURDIR)/cd ~/Espers/src/obj $(BOOST_INCLUDE_PATH) $(BDB_INCLUDE_PATH) $(OPENSSL_INCLUDE_PATH))
LIBS = $(addprefix -L,$(BOOST_LIB_PATH) $(BDB_LIB_PATH) $(OPENSSL_LIB_PATH))

LMODE = dynamic
LMODE2 = dynamic
ifdef STATIC
	LMODE = static
	ifeq (${STATIC}, all)
		LMODE2 = static
	endif
endif

# for boost 1.37, add -mt to the boost libraries
LIBS += \
 -Wl,-B$(LMODE) \
   -l boost_system$(BOOST_LIB_SUFFIX) \
   -l boost_filesystem$(BOOST_LIB_SUFFIX) \
   -l boost_program_options$(BOOST_LIB_SUFFIX) \
   -l boost_thread$(BOOST_LIB_SUFFIX) \
   -l db_cxx$(BDB_LIB_SUFFIX) \
   -l ssl \
   -l crypto

ifndef USE_UPNP
	override USE_UPNP = -
endif
ifneq (${USE_UPNP}, -)
	LIBS += -l miniupnpc
	DEFS += -DUSE_UPNP=$(USE_UPNP)
endif

LIBS+= \
 -Wl,-B$(LMODE2) \
   -l z \
   -l dl \
   -l pthread


# Hardening
# Make some classes of vulnerabilities unexploitable in case one is discovered.
#
    # This is a workaround for Ubuntu bug #691722, the default -fstack-protector causes
    # -fstack-protector-all to be ignored unless -fno-stack-protector is used first.
    # see: https://bugs.launchpad.net/ubuntu/+source/gcc-4.5/+bug/691722
    HARDENING=-fno-stack-protector

    # Stack Canaries
    # Put numbers at the beginning of each stack frame and check that they are the same.
    # If a stack buffer if overflowed, it writes over the canary number and then on return
    # when that number is checked, it won't be the same and the program will exit with
    # a "Stack smashing detected" error instead of being exploited.
    HARDENING+=-fstack-protector-all -Wstack-protector

    # Make some important things such as the global offset table read only as soon as
    # the dynamic linker is finished building it. This will prevent overwriting of addresses
    # which would later be jumped to.
    LDHARDENING+=-Wl,-z,relro -Wl,-z,now

    # Build position independent code to take advantage of Address Space Layout Randomization
    # offered by some kernels.
    # see doc/build-unix.txt for more information.
    ifdef PIE
        HARDENING+=-fPIE
        LDHARDENING+=-pie
    endif

    # -D_FORTIFY_SOURCE=2 does some checking for potentially exploitable code patterns in
    # the source such overflowing a statically defined buffer.
    HARDENING+=-D_FORTIFY_SOURCE=2
#


DEBUGFLAGS=-g


ifeq (${ARCH}, i686)
    EXT_OPTIONS=-msse2
endif


# CXXFLAGS can be specified on the make command line, so we use xCXXFLAGS that only
# adds some defaults in front. Unfortunately, CXXFLAGS=... $(CXXFLAGS) does not work.
xCXXFLAGS=-O2 $(EXT_OPTIONS) -pthread -Wall -Wextra -Wno-ignored-qualifiers -Wformat -Wformat-security -Wno-unused-parameter \
    $(DEBUGFLAGS) $(DEFS) $(HARDENING) $(CXXFLAGS)

# LDFLAGS can be specified on the make command line, so we use xLDFLAGS that only
# adds some defaults in front. Unfortunately, LDFLAGS=... $(LDFLAGS) does not work.
xLDFLAGS=$(LDHARDENING) $(LDFLAGS)

OBJS= \
    cd ~/Espers/src/leveldb/libleveldb.a \
    cd ~/Espers/src/obj/node/alert.o \
    cd ~/Espers/src/obj/consensus/version.o \
    cd ~/Espers/src/obj/consensus/checkpoints.o \
    cd ~/Espers/src/obj/node/netbase.o \
    cd ~/Espers/src/obj/node/addrman.o \
    cd ~/Espers/src/obj/subcore/crypter.o \
    cd ~/Espers/src/obj/subcore/key.o \
    cd ~/Espers/src/obj/util.init.o \
    cd ~/Espers/src/obj/daemon/bitcoind.o \
    cd ~/Espers/src/obj/subcore/keystore.o \
    cd ~/Espers/src/obj/core/chain.o \
    cd ~/Espers/src/obj/core/main.o \
    cd ~/Espers/src/obj/node/net.o \
    cd ~/Espers/src/obj/subcore/protocol.o \
    cd ~/Espers/src/obj/consensus/velocity.o \
    cd ~/Espers/src/obj/rpc/rpcvelocity.o \
    cd ~/Espers/src/obj/rpc/rpcclient.o \
    cd ~/Espers/src/obj/rpc/rpcprotocol.o \
    cd ~/Espers/src/obj/rpc/rpcserver.o \
    cd ~/Espers/src/obj/rpc/rpcmisc.o \
    cd ~/Espers/src/obj/rpc/rpcnet.o \
    cd ~/Espers/src/obj/rpc/rpcblockchain.o \
    cd ~/Espers/src/obj/rpc/rpcrawtransaction.o \
    cd ~/Espers/src/obj/subcore/timedata.o \
    cd ~/Espers/src/obj/subcore/script.o \
    cd ~/Espers/src/obj/core/sync.o \
    cd ~/Espers/src/obj/subcore/txmempool.o \
    cd ~/Espers/src/obj/core/blocksizecalculator.o \
    cd ~/Espers/src/obj/util/util.o \
    cd ~/Espers/src/obj/subcore/hash.o \
    cd ~/Espers/src/obj/subcore/noui.o \
    cd ~/Espers/src/obj/consensus/kernel.o \
    cd ~/Espers/src/obj/primitives/pbkdf2.o \
    cd ~/Espers/src/obj/crypto/scrypt/scrypt.o \
    cd ~/Espers/src/obj/crypto/scrypt/scrypt-arm.o \
    cd ~/Espers/src/obj/crypto/scrypt/scrypt-x86.o \
    cd ~/Espers/src/obj/crypto/scrypt/scrypt-x86_64.o \
    cd ~/Espers/src/obj/core/chainparams.o \
    cd ~/Espers/src/obj/core/blockparams.o \
    cd ~/Espers/src/obj/crypto/common/blake.o\
    cd ~/Espers/src/obj/crypto/common/bmw.o\
    cd ~/Espers/src/obj/crypto/common/groestl.o\
    cd ~/Espers/src/obj/crypto/common/jh.o\
    cd ~/Espers/src/obj/crypto/common/keccak.o\
    cd ~/Espers/src/obj/crypto/common/skein.o\
    cd ~/Espers/src/obj/crypto/common/cubehash.o \
    cd ~/Espers/src/obj/crypto/common/luffa.o \
    cd ~/Espers/src/obj/crypto/common/aes_helper.o \
    cd ~/Espers/src/obj/crypto/common/echo.o \
    cd ~/Espers/src/obj/crypto/common/shavite.o \
    cd ~/Espers/src/obj/crypto/common/simd.o \
    cd ~/Espers/src/obj/crypto/common/hamsi.o \
    cd ~/Espers/src/obj/crypto/common/fugue.o \
    cd ~/Espers/src/obj/crypto/common/shabal.o \
    cd ~/Espers/src/obj/crypto/common/whirlpool.o \
    cd ~/Espers/src/obj/crypto/common/haval.o \
    cd ~/Espers/src/obj/crypto/common/sha2big.o

ifeq (${USE_WALLET}, 1)
    DEFS += -DENABLE_WALLET
    OBJS += \
        cd ~/Espers/src/obj/db.o \
        cd ~/Espers/src/obj/miner.o \
        cd ~/Espers/src/obj/rpcdump.o \
        cd ~/Espers/src/obj/rpcmining.o \
        cd ~/Espers/src/obj/rpcwallet.o \
        cd ~/Espers/src/obj/wallet.o \
        cd ~/Espers/src/obj/walletdb.o
endif

all: Espersd

LIBS += $(CURDIR)/leveldb/libleveldb.a $(CURDIR)/leveldb/libmemenv.a
DEFS += $(addprefix -I,$(CURDIR)/leveldb/include)
DEFS += $(addprefix -I,$(CURDIR)/leveldb/helpers)
OBJS += cd ~/Espers/src/obj/txdb-leveldb.o
leveldb/libleveldb.a:
	@echo "Building LevelDB ..."; cd leveldb; make libleveldb.a libmemenv.a; cd ..;
cd ~/Espers/src/obj/txdb-leveldb.o: leveldb/libleveldb.a

# auto-generated dependencies:
-include cd ~/Espers/src/obj/*.P

cd ~/Espers/src/obj/build.h: FORCE
	/bin/sh ../share/genbuild.sh cd ~/Espers/src/obj/build.h
version.cpp: cd ~/Espers/src/obj/build.h
DEFS += -DHAVE_BUILD_INFO

cd ~/Espers/src/obj/scrypt-x86.o: scrypt-x86.S
	$(CXX) -c $(xCXXFLAGS) -MMD -o $@ $<

cd ~/Espers/src/obj/scrypt-x86_64.o: scrypt-x86_64.S
	$(CXX) -c $(xCXXFLAGS) -MMD -o $@ $<

cd ~/Espers/src/obj/scrypt-arm.o: scrypt-arm.S
	$(CXX) -c $(xCXXFLAGS) -MMD -o $@ $<

cd ~/Espers/src/obj/%.o: %.cpp
	$(CXX) -c $(xCXXFLAGS) -MMD -MF $(@:%.o=%.d) -o $@ $<
	@cp $(@:%.o=%.d) $(@:%.o=%.P); \
	  sed -e 's/#.*//' -e 's/^[^:]*: *//' -e 's/ *\\$$//' \
	      -e '/^$$/ d' -e 's/$$/ :/' < $(@:%.o=%.d) >> $(@:%.o=%.P); \
	  rm -f $(@:%.o=%.d)

cd ~/Espers/src/obj/%.o: %.c
	$(CXX) -c $(xCXXFLAGS) -fpermissive -MMD -MF $(@:%.o=%.d) -o $@ $<
	@cp $(@:%.o=%.d) $(@:%.o=%.P); \
	  sed -e 's/#.*//' -e 's/^[^:]*: *//' -e 's/ *\\$$//' \
	      -e '/^$$/ d' -e 's/$$/ :/' < $(@:%.o=%.d) >> $(@:%.o=%.P); \
	  rm -f $(@:%.o=%.d)

Espersd: $(OBJS:cd ~/Espers/src/obj/%=cd ~/Espers/src/obj/%)
	$(LINK) $(xCXXFLAGS) -o $@ $^ $(xLDFLAGS) $(LIBS)

clean:
	-rm -f Espersd
	-rm -f cd ~/Espers/src/obj/*.o
	-rm -f cd ~/Espers/src/obj/*.P
	-rm -f cd ~/Espers/src/obj/build.h

FORCE:
